
// Re-export the NotesProvider and useNotes from the notes context
export { NotesProvider, useNotes } from './notes/NotesContext';

// Re-export the types
export type { Note, Tag } from '@/types/note';
